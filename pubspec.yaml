name: quarentine_schedule
description: An App for helping students during quarentine.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  url_launcher: ^5.7.2
  flutter_colorpicker: ^0.3.4
  weekday_selector: ^0.4.0
  provider: ^4.3.2+2
  path_provider: ^1.6.18


dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true

  assets:
    - assets/schedule.png

